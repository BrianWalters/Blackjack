/*------------------------------------*\
BOX-SIZING

Sets the default to border-box so layout makes a thousand times more sense.
https://css-tricks.com/box-sizing/

Placeholders:
borderBox
\*------------------------------------*/

$borderBox
    box-sizing border-box

    &:before, &:after
        box-sizing inherit

borderBox()
    @extend $borderBox

if resets
    *
        @extend $borderBox